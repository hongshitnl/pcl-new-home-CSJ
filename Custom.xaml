<local:MyCard Title="欢迎" Margin="0,0,0,0" CanSwap="false" IsSwaped="false">
	<StackPanel Margin="0,0,0,0">
        <TextBlock TextWrapping="Wrap" Margin="215,10,215,10" Foreground="{DynamicResource ColorBrush2}" FontSize="30" HorizontalAlignment="Center"
                    Text="欢迎使用CSJ制作的自制主页！" />
        <TextBlock TextWrapping="Wrap" Margin="250,0,250,10" Foreground="{DynamicResource ColorBrush2}" FontSize="11" HorizontalAlignment="Center"
                    Text="by：CSJ" />
        <Calendar HorizontalAlignment="Center" Margin="0,0,0,0" />
        <TextBlock TextWrapping="Wrap" Margin="0,0,0,10" Foreground="{DynamicResource ColorBrush2}" HorizontalAlignment="Center"
       	 Text="“{cave}”" />
	</StackPanel>
</local:MyCard>
<local:MyCard Title="启动器小工具" Margin="0,10,0,0" CanSwap="true" IsSwaped="false">
	<StackPanel Margin="0,0,0,0">
         <local:MyButton Margin="0,45,0,10" Padding="400,0,400,0" Height="35" HorizontalAlignment="Center"
                    Text="刷新主页" ToolTip="此操作会刷新主页，同时上方的回声洞也会刷新！" EventType="刷新主页" EventData="点击重新加载主页。如果主页文件为联网更新，这会重新下载主页内容"/>
	</StackPanel>
</local:MyCard>